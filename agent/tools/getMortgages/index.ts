// Found in the shared folder as it is required in the serverless calls and the code must live in the assets folder for upload.
export { getMortgages } from '../../../functions/assets/shared/tools/getMortgages.private';
